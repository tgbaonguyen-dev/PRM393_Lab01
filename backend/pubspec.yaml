name: backend
description: A 3-layer Dart Shelf Backend for PRM393 Attendance System
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '^3.0.0'

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  shelf_cors_headers: ^0.1.5
  http: ^1.2.0
  crypto: ^3.0.3

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
